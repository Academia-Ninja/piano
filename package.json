{
  "name": "piano",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build --emptyOutDir",
    "preview": "yarn build && vite preview"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  }
}
